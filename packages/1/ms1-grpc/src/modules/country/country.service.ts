
export class CountryService {

/*   async createOne(data: any) {
    const { countryName, countryCode } = data;
    const entity = Country.build({ countryName, countryCode });
    return await entity.save();
  } */

}
